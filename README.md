# L-i

This repository contains example AutoLISP utilities for AutoCAD.

## Contents

- `lisp/star.lsp` - Command to draw star polygons in AutoCAD. Load the file
  using `APPLOAD` and run the `STAR` command to create custom stars.
